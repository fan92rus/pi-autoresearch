/**
 * Stub for @earendil-works/pi-ai — test process only.
 * StringEnum returns a typebox-style callable proxy (same as Type).
 */
import { Type as _callable } from "./typebox-stub.mjs";

export function StringEnum(..._args) {
  return _callable;
}
